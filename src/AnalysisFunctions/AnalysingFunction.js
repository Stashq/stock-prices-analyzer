class AnalyticalFunction {
    constructor(type, inputs) {
        this.type = type;
        this.inputs = inputs;
      }
    getTraces() {
        throw new Error('Method unimplemented');
    }
}

export default AnalyticalFunction;
